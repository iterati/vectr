add mode type dropdown + add events for it

design mode library
    save modes to mode library (with name)
    displays mode + preview?

design bundle configuration
    drag/drop to list with max of 7/8 items per bundle
    2 bundles to start with

? design mode preview

hook up serial to real light


DONE:
- Pattern Elements
    - Pattern read events

